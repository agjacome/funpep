{
  "http_host"   : "localhost",
  "http_port"   : 8080,
  "http_path"   : "/funpep",
  "http_digest" : true,
  "http_gzip"   : true,

  "clustalo" : "/usr/bin/clustalo",

  "database"  : "database",
  "temporal"  : "database/temporal",
  "job_queue" : "database/job_queue"
}
